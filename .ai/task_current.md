# Current Task

## Task ID
PIPELINE1-CONTINUOUS-NARRATION-006

## Name
Pipeline 1 Voice-Aware Continuous Narration and Bounded Reasoning

## Status
SOURCE_REVIEW_PASS_OWNER_RETEST_READY_STATIC_FINAL_WAITING

## Authority
- Parent failed head: `review/PIPELINE1-FINAL-RUNTIME-GUARDS-005@68c750524f9604b7799d97a2b5604d87368f889c` / PR #46 Owner FAIL.
- Active branch: `review/PIPELINE1-CONTINUOUS-NARRATION-006`.
- Draft PR: #47.
- Reviewed source head: `e07c02776a5918be7a4d2c1a72b26ed3138027cc`.
- PM source review: `4905691792`.
- Spec: `.ai/task_specs/PIPELINE1-CONTINUOUS-NARRATION-006.md`.

## Owner outcome
Keep automatic safe sequential multi-job progression, but generate one coherent narration written for the selected voice/speed and source duration before TTS. Successful final audio must be 95–100% of source duration without cutting voice or changing video speed.

## Implemented flow
1. ASR + adaptive Vision collect evidence unchanged.
2. Final reasoning receives source duration, selected voice and selected speed.
3. Compact final schema returns one `narration_script`; Vision scenes are reused rather than regenerated by qwen.
4. Initial narration character budget targets ~97.5% of the source duration.
5. Short-video global reasoning timeout is 150s; token budget derives from target narration size.
6. Pipeline 1 performs one full-text `/api/tts/generate` request per pass; legacy `/api/tts-retry` is no longer used here.
7. Electron main applies selected speed through ffmpeg `atempo`, persists `voice.wav`, and probes exact duration through ffprobe.
8. If pass 1 is outside 95–100%, measured actual characters/second is used for exactly one whole-narration fit, then one final full-text TTS pass.
9. Pass 2 outside range fails closed and keeps P2 locked.
10. `tts_timed.srt` is derived after continuous voice synthesis for subtitle display only; it does not cause segmented speech generation.
11. Manual Job browsing fix is inherited. Queue runner code is untouched, preserving prior automatic sequential continuation/failure isolation behavior.

## Source scope actually changed
- `src/main/p1-vision-ipc.js`
- `src/main/preload.js`
- `src/renderer/js/pipeline1-analysis.js`
- `src/renderer/js/pipelines/pipeline1-ai.js`

No P2/P3/STTN/Settings/backend/TTS-engine source change.

## Verification completed
- Local reconstructed `node --check` PASS for the four changed JS source candidates.
- Deterministic narration budget, measured repair budget, speed clamp and reasoning timeout helpers: PASS.
- Source review confirms final schema contains one narration string and no final reasoning `scenes`/`script_segments` regeneration.
- Source review confirms P1 continuous path has no `/api/tts-retry` and one `/api/tts/generate` call site per TTS pass.
- Source review confirms only one whole-narration duration fit + one re-TTS path.
- PM GitHub source/diff review PASS `4905691792`.
- GitHub CI/status: none configured; unresolved review threads: none.

## Verification still required
- Exact `node --check` on all four files at the final docs/head tested by Owner.
- Exact `git diff --check 68c750524f9604b7799d97a2b5604d87368f889c..HEAD`.
- Owner real-app: former overlong 24.30s case must produce one continuous speech track and final 95–100% ratio or fail closed after one fit.
- Owner real-app: second queued Job auto-starts after first completes/fails; no queue stall.
- Owner real-app: another Job may remain selected for inspection while current Job runs.
- Owner real-app: short-input qwen global reasoning obeys the new 150s bound rather than old ~6-minute behavior.
- Pending final adaptive coverage: >60s input demonstrates >8 samples/multiple <=8-frame Vision chunks before ultimate P1 merge approval.

## Gates
Execution PASS; automated/static PARTIAL; code review PASS; Owner NOT STARTED — READY; documentation sync PASS after final docs publication; merge BLOCKED; Step 3 BLOCKED.
