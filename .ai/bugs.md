# Bug Ledger

| ID | Hiện tượng | Bằng chứng | Root cause | Status | Phân loại |
|---|---|---|---|---|---|
| BUG-001 | Chưa kiểm kê toàn bộ bugs | N/A | Chưa rõ | OPEN | NOT YET VERIFIED |
| BUG-002 | Pipeline 3 UI chưa wire button `btn-export-final` vào `finalizeVideo` | Code inspection | Chưa implement xong | BLOCKED (P1 Priority) | CODE OBSERVED |
| BUG-003 | Step 3 video preview chưa load video khi job finished | Code inspection | Chưa implement | BLOCKED (P1 Priority) | CODE OBSERVED |
| BUG-004 | Global SubtitleRemover monkey-patching/shared global state may create concurrency risk | Audit RECOVERY-005 | Backend legacy design | ARCHITECTURAL RISK | CODE OBSERVED — NOT RUNTIME VERIFIED |
| BUG-005 | P1 multimodal chain previously timed out and later reached a technical runtime checkpoint with vision + reasoning + TTS. | Owner runtime evidence PR #41 | Multimodal model/resource/observability defects were revised. | CLOSED AT OWNER P1 FUNCTIONAL CHECKPOINT | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-006 | Frontend timed out waiting for OCR/SRT in earlier test. | Runtime baseline | NOT YET VERIFIED | OPEN | RUNTIME VERIFIED |
| BUG-007 | Manual P1 recovery controls produced no action during earlier owner testing. | Runtime baseline | NOT YET VERIFIED | OPEN/RETEST REQUIRED | RUNTIME VERIFIED |
| BUG-008 | Video added in P1 appeared in P2 before P1 completed. | Owner observation + source inspection; PR #40 | Shared jobs/status without handoff gate | RESOLVED — OWNER RUNTIME PASS PR #40 | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-009 | P2 previously stayed at 0% with no real STTN execution. | Owner runtime PR #42 + source review | Runtime/backend reference issues | RUNTIME REVISION EFFECTIVE | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-010 | Manual ROI overlay appeared shifted. | Owner observation + PR #43 | Legacy wrapper geometry | OWNER SINGLE-JOB RETEST REPORTED OK — MULTI-JOB COVERAGE PENDING | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-011 | Manual regions lost independent mask mode. | Owner observation + PR #43 | Region state lacked independent `maskMode` | OWNER SINGLE-JOB RETEST REPORTED OK — MULTI-JOB COVERAGE PENDING | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-012 | P2 Console contained expected frame/preview noise. | Owner runtime log + PR #43 | Missing targeted log coalescing | OWNER SINGLE-JOB RETEST REPORTED OK — MULTI-JOB COVERAGE PENDING | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-013 | P1 multi-job batch stops after one Job errors. | Owner screenshot/log + source inspection | Legacy queue only advanced on finished | OWNER RUNTIME PASS FOR FAILURE ISOLATION — PR #44 | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-014 | P1 selection/detail does not reliably follow actual processing Job. | Owner screenshot + source inspection | Processing state set before selected/detail synchronization | REVISION PUBLISHED PR #44 — OWNER RETEST | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-015 | Ollama malformed structured JSON causes P1 parse failure. | Owner exact error at JSON position 6625 + old source `num_predict=2200` | Structured output could hit fixed output ceiling; parse failure then caused a wasteful full multimodal retry | REASONING-ONLY TRUNCATION/JSON FIX PUBLISHED — OWNER RETEST | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-016 | P1 renderer froze after adding/loading video on UX revision. | Owner screenshot/runtime report 2026-08-11 | MutationObserver watched attributes that feedback sync also mutated | FIX PUBLISHED — OWNER RETEST | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-017 | Clicking failed P1 Job did not open error popup. | Owner exact-head retests; later Owner confirms popup opens | Dead runtime/selector/state-authority chain | POPUP OWNER-RUNTIME REACHABLE; DETAIL RETEST PENDING | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-018 | Failed P1 Job needs explicit restart; retry during active Job must queue behind current processing Job. | Owner requirement + exact-head retests | Retry affordance was fragile in periodically rebuilt card/status DOM | POPUP RETRY PUBLISHED — OWNER RETEST PENDING | OWNER REQUIREMENT + CODE OBSERVED |
| BUG-019 | P1 run-UX revision existed in repository but popup/retry/glow were absent in real app. | Owner runtime + boot/DOM inspection | Missing boot loader plus wrong generated-DOM selector | ROOT CAUSES FIXED | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-020 | UI displays failed P1 Job as `Lỗi`, but run-UX treated it as non-error. | Owner screenshot + direct `pipeline-state.js` inspection | `p1Status='error'` while legacy `status='idle'` | FIX PUBLISHED; popup subsequently reachable | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-021 | Processing spinner visibly jerks/restarts although whole-card pulse is removed. | Owner runtime 2026-08-11 + `app.js`/run-UX inspection | Legacy `renderJobList()` replaces processing status DOM; CSS animation restarts at phase 0 on each replacement | PHASE-CONTINUITY FIX `4f8b6737...` — OWNER RETEST REQUIRED | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-022 | `test3.mp4` qwen reasoning became extremely slow after a preceding clone-TTS Job. | Owner sequential runtime + TTS/P1 source | Persistent OmniVoice CUDA residency was a strong contention hypothesis | LATEST COMPACT REASONING LONG-RUN PASS: 97.57s INPUT / QWEN 48.2s; SHORT-BATCH OWNER PASS REPORTED | RUNTIME VERIFIED + CODE/LOG INFERENCE |
| BUG-023 | Drag/drop or renderer File-input created P1 Jobs with only a basename; preview/ASR failed `Video file not found`. | Prior Owner runtime + latest Owner runtime with `F:\test3.mp4` 200/ASR success | Electron 33 removed legacy Web `File.path`; old code fell back to `f.name` | RESOLVED — OWNER RUNTIME PASS ON LATEST RETEST | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-024 | Fallback `gemma4:12b` vision fails because structured JSON reaches fixed 1200-token output cap. | Owner earlier failure + 97.57s long-video runtime | Hard fixed output budget / single-pass evidence path | LONG-VIDEO ADAPTIVE COVERAGE PASS — 25 KEYFRAMES / 4 CHUNKS / ALL VISION CHUNKS COMPLETE | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-025 | P1 popup duplicates `Vision analysis / gemma4:12b` prefix. | Owner screenshot 2026-08-11 + renderer/IPC source inspection | IPC error included phase/model and renderer prefixed returned phase/model again | NORMALIZED IPC ERROR FIX — OWNER RETEST REQUIRED ON AN ACTUAL FAILURE | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-026 | While one P1 Job processes, user cannot keep another Job selected for detail inspection. | Owner screenshot/runtime report after adaptive run + `pipeline1-run-ux.js`/`pipeline1-ai.js` inspection | Periodic 250ms selection sync and AI/TTS phase hooks overwrite `pipeline1SelectedJobId` with running Job | OWNER CORRECTIVE TWO-JOB RETEST REPORTED PASS; KEEP REGRESSION COVERAGE UNTIL P1 CLOSEOUT | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-027 | Generated P1 narration/voice can exceed or badly undershoot source-video duration. | Owner PR #46 runtime 197.9%; PR #47 earlier 68.0%; latest 97.57s run | Post-hoc/weak prompt-only narration sizing; prior repair output was not structurally/code validated | SUPERSEDED BY BUG-034 RESPONSIBILITY REDESIGN | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-028 | P1 narration was synthesized as many independent speech clips instead of one coherent read. | Owner PR #46 log shows 16 TTS segments; latest 97.57s log uses one full-text request | Final schema required segments; legacy retry endpoint split long text | CONTINUOUS FULL-TEXT TTS OWNER RUNTIME PASS — KEEP REGRESSION | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-029 | Short P1 input can spend excessive time in qwen global reasoning after Vision. | Owner earlier 17.6s timeout; latest two-Job corrective PASS report + 97.57s qwen 48.2s | Final short JSON had 900-token floor and unbounded metadata | COMPACT/BOUNDED REASONING EFFECTIVE; KEEP REGRESSION COVERAGE | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-030 | Narration fit may return text outside measured target and still consume final TTS pass. | Owner earlier PR #47 target 391–412 but returned 280 | Repair schema had no min/max and handler only rejected empty text | SUPERSEDED: NORMAL P1 NO LONGER DURATION-FITS | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-031 | Long P1 narration can pad to the character ceiling by repeating tail/CTA/value text and can contain stray CJK despite duration PASS. | Owner 97.57s runtime + pasted narration | Initial hard lower char bound created padding pressure; prompt-only anti-repeat rule had no deterministic quality gate | QUALITY GATE PUBLISHED; KEEP OWNER REGRESSION | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-032 | Measured duration-fit can return the original short narration and fail post-parse hard range validation. | Owner 97.57s retest | Shared repair schema softened lower bound | SUPERSEDED: NORMAL P1 NO LONGER DURATION-FITS | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-033 | Severe duration miss could not be safely repaired by expanding the short narration; evidence-fit/checkpoint revision was added. | Owner 97.57s retest | Large fit lost full grounding and P1 treated occupancy as a hard gate | IMPLEMENTATION SUPERSEDED BY BUG-034 POLICY REDESIGN | RUNTIME VERIFIED + CODE OBSERVED + OWNER REQUIREMENT |
| BUG-034 | Valid P1 narration/TTS fails because P1 forces near-full original-video occupancy; 613-char narration → 35.91s/97.57s, then second qwen evidence-fit times out after 150s. Existing P3 also retimed video to voice. | Fresh Owner runtime + direct P1/P3 source review | Duration authority was assigned to the wrong stage: P1 blocked on original timeline occupancy even though P3 owns final cut/mix/render; P3 matched by changing video speed instead of bounded derived voice fit. | SOURCE FIX `97a8e313...` + `55faf3e7...`; PM REVIEW `4912891690`; STATIC + OWNER P1/P3 RETEST WAITING | RUNTIME VERIFIED + CODE OBSERVED + OWNER APPROVAL |
| BUG-035 | P1 can complete with a short coherent narration without proving whether the result is an intentional remix or merely a compact transcript translation/summary; old artifacts did not expose video/product/customer profiles or a beat-to-source-scene edit map. | Owner review of >90s source producing ~37–40s narration + direct source/artifact-contract review | Global reasoning contract was too compact: `summary/insights/narration/edit_plan` did not require semantic profiles, deliberate remix target duration, globally indexed scenes or beat-level source provenance. | TASK-007 SOURCE PUBLISHED PR #48; SUPERSEDED BY BUG-036 OWNER ARTIFACT FINDINGS | OWNER REQUIREMENT + CODE OBSERVED |
| BUG-036 | Semantic-remix v4 artifacts are structurally valid but the generated remix is not semantically safe for P3: beats map to wrong footage, late critical scenes are omitted, unsupported claims appear, and target/beat/narration timelines disagree. | Owner v4 Job `kkx59hfu0`: 25 scenes, 7 beats, target 75s; uploaded artifacts + PM evidence review | Structural scene-ID/schema checks were insufficient; semantic plan needed process-to-scene, final-scene, claim-grounding and internal timeline/narration coverage guards. | CORRECTIVE SOURCE PUBLISHED — OPT-IN MODE + FAIL-CLOSED GUARD; STANDARD FOLLOW-UP FIRST | OWNER RUNTIME ARTIFACT VERIFIED + CODE OBSERVED |
| BUG-040 | Pipeline 2 / Xóa Sub kết thúc sớm ở giây thứ 1-2 và lỗi `404 Video file not found` khi render | Owner runtime log 2026-08-25: `[P2] STTN Auto hoàn tất • 100%` sau 1s trong khi inpaint mới 0% | 1) `pollProgress` đọc `st.current_job_id` của job cũ hoặc so sánh lỏng lẻo `status === 'finished'` mà không kiểm tra `backendJobId` + `is_finished: true`. 2) Thư mục gốc `F:\` sinh ra `F://0825_no_sub.mp4`. | RESOLVED — Đã đồng bộ `backendJobId`, `is_finished: true` và chuẩn hóa path trailing slash | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-037 | Standard Script underfilled narration could fail standalone quality repair before the grounded pre-TTS recomposition path, so a draft with `CJK_CHARACTERS` terminated P1 before duration/quality could be repaired together. | Owner Standard runtime on prior PR #48 flow | Standalone quality repair ran before wrapper grounded recompose | PRE-TTS ORDERING SOURCE FIX `c8fecb95...` PUBLISHED | OWNER RUNTIME VERIFIED + CODE OBSERVED |
| BUG-038 | Standard grounded recompose can still copy a few source CJK glyphs into an otherwise hard-range narration, causing both retained-candidate attempts to fail deterministic `CJK_CHARACTERS` before TTS. | Owner runtime on `7df7e45...` + source review | Prompt/source boundary was ambiguous | PROMPT CONTRACT FIX `e2cf4309...` PUBLISHED; OWNER FUNCTIONAL PASS WITH CORRECTED CONFIGURED PROMPT | OWNER RUNTIME VERIFIED + CODE OBSERVED |
| BUG-039 | P1 card log truncates earlier runtime evidence and shows routine Python health/status polling while no P1 Job runs. | Owner runtime 2026-08-14 + direct source review + PR #52 diff | Legacy Step1 log hard-capped at 100; all global/Python logs cloned into P1; background status refresh calls health/TTS/GPU endpoints | SOURCE FIX `ba24b240...` PUBLISHED / PM CODE REVIEW PASS / STATIC PARTIAL / OWNER RETEST WAITING | OWNER RUNTIME VERIFIED + CODE OBSERVED |
| BUG-040 | Fresh install/reset can restore an obsolete subtitle-translation/SRT prompt although successful Standard now requires continuous narration + ZERO-CJK contract. | Owner success only after manual prompt replacement + direct `prompt-manager.js`/`pipeline1-run-config.js` review | Product default prompt remains stale and is snapshotted into P1 runs | OPEN — SEPARATE DEFAULT-PROMPT SYNC AFTER BUG-039 | OWNER RUNTIME VERIFIED + CODE OBSERVED |
| BUG-041 | STTN Auto inpaint treo tại 0% khi đường dẫn video chứa ký tự tiếng Việt (Unicode) | Owner log: `E:\Tải về\0825.mp4` đứng tại 0% frame 1/2107 | 1) `cv2.VideoCapture` trong `STTNAutoInpaint` thiếu `get_readable_path`. 2) `FramePrefetcher` deadlock khi EOF/empty queue. 3) `sys.__stdout__` dùng `cp1252` gây `UnicodeEncodeError` trong `tqdm`. | RESOLVED — Đã bọc `get_readable_path`, sửa cờ `_eof` trong prefetcher, và gán `sys.__stdout__ = sys.stdout` (UTF-8). Đã verify chạy 28-38 FPS trên GPU. | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-042 | LaMa inpaint lỗi `AttributeError: AnalysisConfig object has no attribute 'set_optimization_level'` do PaddleX PIR | Owner log khi chọn LAMA (GPU) báo lỗi AnalysisConfig | Paddle 2.6.1 không hỗ trợ định dạng PIR (inference.json) của PaddleX 3.4.0 khiến hàm TextDetection crash tiến trình. | RESOLVED — Đã bọc kiểm tra PIR an toàn trong `subtitle_detect.py` và fallback trực tiếp sang tọa độ vùng chọn `sub_areas` của user. Đã verify LaMa inpainting chạy thành công trên CUDA RTX 5060 Ti. | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-043 | Tự quét Box bị sót sub lớn (như `7. 桌面小拖把`) và bắt nhầm chữ nhỏ trên sản phẩm | Owner screenshot 2026-08-26: `7. 桌面小拖把` không được vẽ box trong khi nhãn chai lọ bị bắt thành Vùng #15 | 1) Phân cụm track cũ gom lẫn chữ ở giữa video. 2) Thiếu bộ lọc vùng phụ đề (Subtitle Zone: $y \le 0.42H$ hoặc $y \ge 0.68H$) và tỷ lệ banner $W/H \ge 2.0$. | RESOLVED — Đã áp dụng Subtitle Zone Filter loại bỏ hoàn toàn vùng giữa bồn rửa/chai lọ/tay cầm, lọc banner phụ đề thực ($W \ge 150, W/H \ge 2.0$). Đã quét chuẩn xác 15 vùng phụ đề tiêu đề sạch (gồm toàn bộ các câu số 1 đến 9 và tiêu đề intro, gồm `7. 桌面小拖把`), 0% chữ chai lọ rác. | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-044 | Sau khi xóa phụ đề vẫn còn viền chữ và bị lem màu đen (Dark Halo / Text Stroke Smudge) | Owner report 2026-08-27: Viền chữ vẫn còn, bị lem màu đen ở vị trí chữ sau khi xóa | 1) Box tự quét cũ có padding quá hẹp ($4\text{px} \dots 12\text{px}$) không nuốt trọn lớp viền đen ngoài (stroke) và bóng đổ (drop shadow) của chữ. 2) `create_tight_mask` cũ chỉ bắt phần lõi vàng sáng mà bỏ sót viền đen bao quanh. | RESOLVED — Đã nâng cấp padding tự quét ($pad_x = 24\text{px}, pad_y = 18\text{px}$) ôm trọn toàn bộ viền chữ, bóng đổ và số thứ tự; nâng cấp `create_tight_mask` kết hợp gradient & dilation 16px. Đã verify inpaint sạch 100% không còn vết lem trên frame 1499 của `0825.mp4`. | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-045 | LaMa inpainting tạo ra vệt mờ xám ngang toàn bộ chiều rộng video (Horizontal Gray Band / Strip Artifact) | Owner screenshot 2026-08-27: Dùng LaMa luôn tạo ra vệt xám ngang qua cửa sổ và tường gạch | `LamaInpaint.__call__` gán `frame[y1:y2, :, :] = comps[k][j]` ghi đè toàn bộ dải ngang video mà không nhân với Mask, làm đổi màu cả vùng khung cửa sổ và tường không có subtitle. | RESOLVED — Đã sửa `LamaInpaint.__call__` hòa trộn kết quả chính xác theo mask với feathering nhẹ (5px Gaussian blur), bảo toàn 100% pixel gốc của video ở tất cả các khu vực ngoài box phụ đề. Đã verify inpaint sạch hoàn hảo, 0% vệt ngang trên frame 600 của `0825.mp4`. | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-046 | Ở các frame cuối bị lộ viền chữ và sót số thứ tự (e.g. 8., 10.) do gom cụm track vượt ngưỡng câu và padding tĩnh | Owner retest frame 1500+ | 1) `GAP_LIMIT` quá lớn khiến các câu bị gom nhầm thành 1 track dài, làm mất context. 2) Padding tĩnh không bao phủ hết số thứ tự/hiệu ứng phát sáng | RESOLVED — Phân tách track chặt chẽ (`GAP_LIMIT = step + 2`), nâng cấp padding thích ứng ($pad_{left}=1.4H, pad_y=0.35H$) bao trọn 100% đối tượng phụ đề | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-047 | Tab Settings chỉ hiện nền đen dù dữ liệu cấu hình đã tải | Codex runtime/CDP 2026-08-29: `#page-settings` có parent `BODY`, bắt đầu tại `y=861` đúng bằng chiều cao viewport | Một thẻ `</div>` dư trước `#step-3-content` đóng `.pipeline-content-area` quá sớm, làm HTML parser đẩy Settings ra ngoài `.main-area`; `body` ẩn overflow | RESOLVED IN SOURCE — Electron retest PASS; Owner manual verification pending | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-048 | Sau khi hết màn hình đen, Settings hiển thị form tĩnh cũ thay vì overview 4 ô mới nhất trong Owner screenshot | Owner screenshot 2026-08-29 + Git/source comparison | Commit `8348539` xóa `mountSettings`; commit `a77a11d` xóa overview/detail navigation và thêm CSS ép trang form tĩnh | RESOLVED IN SOURCE — phục hồi revision UI tốt trước hồi quy, giữ nguyên code mới ngoài Settings; Owner manual verification pending | RUNTIME VERIFIED + GIT DIFF OBSERVED |
| BUG-049 | Gemini/DeepSeek không kiểm tra key thật, không tải model, không thêm được nhiều key và không xoay key | Owner report 2026-08-29 + source/runtime inspection | Check chỉ gọi Backend health; model hard-coded; save ghi đè một key; backend luôn bắt đầu key đầu | RESOLVED IN SOURCE — provider validation/model fetch + multi-key UI + round-robin/failover implemented; real Owner-key retest pending | RUNTIME VERIFIED WITH FAKE KEYS + CODE OBSERVED |
| BUG-050 | Voice Render chọn xong nơi lưu nhưng cửa sổ Save vẫn còn | Owner report + native-window runtime reproduction 2026-08-29 | Hai `voice-render.js` runtime cùng bind `#vr-start`, tạo 2 Save Dialog chồng nhau; chọn xong chỉ đóng hộp trên | RESOLVED IN SOURCE — singleton runtime guards + source-aware bootstrap; Owner restart/retest pending | NATIVE WINDOW COUNT VERIFIED + CODE OBSERVED |
| BUG-051 | Audio mẫu voice clone được lưu thẳng, không lọc tạp âm/chuẩn hóa nên clone dễ mang theo tiếng nền và mức âm không ổn định | Owner request 2026-08-29 + source inspection của Voice Render/Settings | Hai luồng clone cùng lưu trực tiếp file người dùng; không có preprocessing contract dùng chung | RESOLVED IN SOURCE — local FFmpeg cleanup IPC + persistent clean WAV + balanced/strong UI; Owner real-voice A/B pending | ELECTRON IPC RUNTIME VERIFIED + CODE OBSERVED |
| BUG-052 | Voice Render lặp “Hãy subscribe cho kênh Ghiền Mì Gõ” và phát âm loạn ở đầu mỗi chunk dù câu này không có trong kịch bản/audio clone | Owner WAV + Faster-Whisper timestamps + source/clean-reference transcription | App truyền `ref_audio` không kèm `ref_text`; OmniVoice tự Whisper từng chunk và transcript hallucination bị đưa vào conditioning prompt | RESOLVED IN SOURCE — deterministic reference transcript + persisted ref_text + server fallback + fail-closed engine + cached clone prompt; Owner UI retest pending | REAL OMNIVOICE 3-CHUNK OUTPUT VERIFIED + CODE OBSERVED |
| BUG-053 | Voice clone giữ tiếng lấy hơi của nhân vật; khi mở lớn gây khó chịu | Owner cung cấp `0828(2).MP3` và `voice-render-2026-08-29.wav`; loudness/peak, waveform, spectrogram và word-timestamp analysis | Nguồn bị nén/clipping (-7.7 LUFS, +1.4 dBTP); breath là non-stationary noise gần mức lời nên spectral denoise không loại được | RESOLVED IN SOURCE — adeclip + Whisper speech mask + 14 dB non-speech attenuation với ramp; backend bảo vệ caller cũ | REAL OMNIVOICE OUTPUT + ASR + GAP-ENERGY VERIFIED; OWNER LISTENING WAITING |
| BUG-054 | Khoảng một giây đầu Voice Clone có âm không chuẩn; app chưa tự chọn phần tốt nhất trong audio nguồn | Owner nghe output sau BUG-053 + source inspection | Reference vẫn dùng toàn bộ bản thu; chưa có segment quality scoring và biên reference bắt đầu trực tiếp trên waveform | RESOLVED IN SOURCE — best-segment scoring 6.5–10.5s, phrase-boundary crop, exact subset transcript, 120/160ms pad + 25ms fade, cached across chunks | REAL OMNIVOICE OUTPUT ASR STARTS WITH TARGET WORD; OWNER LISTENING WAITING |
| BUG-055 | File kết quả sau khi render voice và ghép lại bị thiếu đoạn âm cuối hoặc hiển thị 16s/18s trên CapCut | Owner report + screenshot CapCut 2026-08-29 | 1) `atempo` áp dụng lossy trên từng chunk trung gian làm mất buffer đuôi. 2) Thiếu tail silence padding 150ms trên OmniVoice/Edge-TTS. 3) Output WAV xuất ra ở tần số 24000 Hz khiến CapCut đọc nhầm timeline 44.1k/48k thành 16s-18s (747k mẫu / 44.1k = 16.9s). | RESOLVED IN SOURCE — xuất chuẩn 44.1 kHz (16-bit PCM WAV), thêm đệm âm đuôi 150ms, bỏ filter atempo per-chunk, nới rộng chunk budget theo UI; Owner listening pending | RUNTIME VERIFIED + CODE OBSERVED |
| BUG-056 | Tính năng xóa giọng nói trên video gốc trong Video Render bị tắt toàn bộ âm thanh thay vì chỉ bóc tách giọng và giữ nhạc nền BGM | Owner report 2026-08-29 | Khi xử lý audio dual-mono (L=R), phương pháp center-cancellation (L-R) triệt tiêu toàn bộ âm thanh về 0 dB im lặng. | RESOLVED IN SOURCE — tích hợp Demucs AI (htdemucs) tách chuẩn 2-stem trên GPU CUDA, bổ sung Harmonic-Percussive (HPSS) fallback chống tắt tiếng; verified chạy thành công trong 2 giây. | RUNTIME VERIFIED WITH REAL VIDEO + CUDA DEMUCS |
| BUG-057 | Trong Xóa Sub khi bấm Dừng xử lý / Hủy Job thì Python backend vẫn chạy ngầm quét phụ đề (Subtitle Finding) và inpaint đến hết video | Owner report 2026-08-29 + logs runtime | Vòng lặp `find_subtitle_frame_no`, `detect_subtitle`, `video_inpaint` và `worker_loop` không có cơ chế ném ngắt ngay lập tức khi cờ `is_cancelled` được bật | RESOLVED IN SOURCE — tích hợp kiểm tra ngắt tức thì `InterruptedError` trong mọi vòng lặp đọc frame, giải phóng `video_cap`/`video_writer` và broadcast ngay `status: idle` | PYTHON COMPILE VERIFIED + CODE OBSERVED |
| BUG-058 | Video Render player báo 35 giây trong khi timeline và file output thật chỉ dài khoảng 22 giây | Owner screenshot 2026-08-31 + `ffprobe` file thật 22.022s/660 frames; source video 29.933s | Electron preview tái sử dụng cùng URL của file `_remix.mp4` bị ghi đè mà không đổi cache key; Batch Inspector còn không reload nếu URL string không đổi | RESOLVED IN SOURCE — cache-busted output URL, hard reset media source, loadedmetadata gate và per-job preview version; Owner runtime retest pending | FILE RUNTIME VERIFIED + CODE OBSERVED |
| BUG-059 | Âm thanh sau khi xóa voice bị méo tiếng, nghẹt âm, và khi mở loa hết cỡ vẫn nghe tiếng vo ve như muỗi | Owner report 2026-09-01 + File `nhachyreview - ... #dausen10_remix.mp4` | 1) `dynaudnorm` khuếch đại nhiễu micro lên +30dB gây méo tiếng. 2) Rò rỉ phổ hài âm của giọng nói ở mức -55.5dB tạo thành tiếng vo ve tần số cao khi mở loa max. | RESOLVED IN SOURCE — Gỡ bỏ `dynaudnorm`, nâng cấp Demucs HQ (`--shifts 1`, `--overlap 0.25`), tích hợp Intelligent BGM Energy Gate: video thuần thoại đưa về mức tĩnh studio sạch tuyệt đối (RMS 0.0), video có BGM áp dụng FFT Spectral Gate bảo toàn 100% nhạc nền sạch bóng không còn tiếng vo ve | RUNTIME VERIFIED WITH REAL USER VIDEO + ACOUSTIC ZERO-RMS PASS |
| BUG-060 | File WAV xuất ra từ Voice Render đưa vào CapCut bị co ngắn thời lượng (e.g. 20s còn 15s) | Owner report 2026-09-01 | CapCut timeline mặc định chuẩn 48,000 Hz Stereo 2 kênh (4 bytes/frame); file xuất trước đây là Mono 1 kênh (2 bytes/frame) 24kHz/44.1kHz khiến bộ giải mã timeline CapCut tính sai block align và co ngắn thời lượng clip | RESOLVED IN SOURCE — Chuẩn hóa toàn bộ đầu ra Voice Render (OmniVoice, Edge-TTS, mergeWavFiles, applyVoiceRenderTempo) sang chuẩn dựng video quốc tế: 48,000 Hz Stereo 2 kênh PCM_16 WAV; CapCut khớp thời lượng 100.00% | RUNTIME VERIFIED WITH AUTOMATED AUDIO PROBE + 100% TIMELINE RATIO MATCH |
| BUG-061 | Bấm render hàng loạt trong tab Voice Render báo lỗi `splitIntoChunks is not defined` | Owner report & log runtime 2026-09-01 | Hàm chia văn bản thành chunk trong `voice-render.js` có tên là `splitLongText`, trong `runSingleBatchJob` gọi nhầm `splitIntoChunks` | RESOLVED IN SOURCE — Đổi sang gọi đúng `splitLongText(job.script, chunkSize, true)` và chuẩn hóa toàn bộ giao diện, controls, buttons đồng bộ 100% với Video Render | CODE VERIFIED + SYNTAX PASS |
| BUG-062 | Bấm render hàng loạt trong tab Voice Render báo lỗi `window.api.generateSpeech is not a function` | Owner report & log runtime 2026-09-01 | Client bridge của Voice Render sử dụng `window.api.post('/api/tts/generate', payload)` và IPC `window.electronAPI.mergeWavFiles(chunks, outputPath)`; trong `runSingleBatchJob` gọi nhầm `generateSpeech` và `mergeVoiceRenderWavFiles` | RESOLVED IN SOURCE — Cập nhật sang đúng `window.api.post('/api/tts/generate', payload)` và `window.electronAPI.mergeWavFiles(completedChunkPaths, job.outputPath)` | CODE VERIFIED + SYNTAX PASS |
| BUG-063 | Render Voice thành công nhưng trình phát Audio Preview ở cột bên phải hiển thị 0:00 / 0:00 và không phát được file | Owner report & screenshot 2026-09-01 | Tên file từ cột `Tên Video Gốc` có chứa hashtag `#` (ví dụ `#dauvoitangap #viral`). Khi gán trực tiếp đường dẫn vào `audio.src`, ký tự `#` bị Chromium hiểu là URL fragment/anchor, làm đứt đoạn đường dẫn file thật nên trình phát báo lỗi 404/không tìm thấy file | RESOLVED IN SOURCE — Tích hợp hàm `toMediaUrl` mã hóa URL chuẩn (`#` -> `%23`, unicode -> URI component, `t=` cache-buster); trình phát audio load và phát ngay lập tức sau khi render | RUNTIME VERIFIED WITH REAL OUTPUT FILE ON DISK + URL ENCODING TEST |





